import { createDisciplineWorkspaceConfig } from "./createDisciplineWorkspaceConfig";

export const renewableDisciplineConfig = createDisciplineWorkspaceConfig({
  id: "renewable-energy",
  name: "Renewable Energy",
  shortName: "Renewable Energy",
});
